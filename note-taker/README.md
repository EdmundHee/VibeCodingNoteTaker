# Pagewise

A calm home for the things you think.

## Setup

1. Create a Supabase project
2. Run `supabase-setup.sql` in the SQL Editor
3. Copy `.env.example` to `.env` and fill in your Supabase credentials
4. Run `npm install && npm run dev`